public class Abstraction {

    public static void main(String[] args) {

        System.out.println("Learning Topic: Abstraction");
        //abstraction is the process of hiding the implementation details and showing only the functionality to the user
        //abstraction can be achieved using abstract classes and interfaces
        //abstract class: a class that cannot be instantiated and can contain abstract methods (methods without a body) and concrete methods (methods with a body)
        //interface: a collection of abstract methods that can be implemented by any class, regardless of its position in the class hierarchy
        //abstraction is used to achieve loose coupling and to hide the complexity of the system from the user
        //abstraction allows us to focus on what the object does instead of how it does it, which makes our code more flexible and easier to maintain
        //abstraction is one of the four fundamental OOP concepts, along with encapsulation, inheritance, and polymorphism  
        

    }
}
